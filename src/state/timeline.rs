//! Timeline state — currently minimal; the heavy lifting lives in
//! `EditorState.timeline` and the UI `ui::timeline` module.

// Re-export the timeline state type from `editor` for convenience.
pub use super::editor::TimelineState;
